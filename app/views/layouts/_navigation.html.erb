<nav  class="navbar tx_navbar navbar-static">
  <div class="container">
    <div class="collapse navbar-collapse bs-example-js-navbar-collapse">

      <% if user_signed_in?  %>

          <% if @role == 'admin' %>

              <%= render 'layouts/super_admin' %>

          <% else %>

              <%= render 'layouts/after_signin' %>

          <% end %>

      <% else %>

          <% if show_colored_header? %>
              <%= render 'layouts/marketrex_public_header' %>
           <% else %>
              <%= render 'layouts/before_signin' %>
          <% end %>
      <% end %>

    </div>
  </div>
</nav>
