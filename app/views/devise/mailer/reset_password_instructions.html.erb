<%= render 'partials/email_header' %>

<h1 class="email-main-hadding">Hello <%= @resource.name %>, </h1>

<div class="email-content">

    <p>There has been a request to reset your password. You can do this through the link below:</p>

    <p><%= link_to 'Change my password', edit_password_url(@resource, reset_password_token: @token) %></p>

    <p>If you didn't request this change, please ignore this email. Your password won't be reset until you access the link above and create a new one.</p>

</div>

<%= render 'partials/email_footer' %>