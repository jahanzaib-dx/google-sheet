<%= render 'partials/email_header' %>


      <h1 style="font-family: arial;font-size: 18px;padding: 13px 0;">Hi <%= @resource.name %>, </h1>

      <div class="email-content">
            <p>Please confirm your account through the link below:</p>

            <p><%= link_to 'Confirm my email', confirmation_url(@resource, confirmation_token: @token) %></p>

      </div>


<%= render 'partials/email_footer' %>